export default {
  setShareDetails(state, val) {
    state.shareDetails = val
  },
  // 写入用户好友数据
  setFollows(state, val) {
    state.follows = val
  },
  setType(state, val) {
    state.type = val
  }
}
